# AutoClickerCtS - Умный автокликер

Многофункциональный автокликер с поддержкой OCR распознавания текста, обработкой всплывающих окон и автоматической покупкой улучшений.

## 🚀 Возможности

- **F6 - Свободный клик**: кликает там, где находится мышь
- **F4 - Умный режим**: автоматически закрывает всплывающие окна и покупает улучшения
- **F1-F3 - Режимы-заглушки** (для будущих обновлений)
- **F5 - Пауза/Продолжить**
- **F9 - Выход**

## 📦 Установка

### Шаг 1: Клонирование репозитория
```bash
git clone https://github.com/ваш-username/AutoClickerCtS.git
cd AutoClickerCtS
```

### Шаг 2: Установка Python (если не установлен)
Скачайте Python 3.8+ с [python.org](https://www.python.org/downloads/)

### Шаг 3: Установка Tesseract OCR (ОБЯЗАТЕЛЬНО!)

#### Для Windows:
**Способ 1 - автоматический:**
```bash
setup_tesseract.bat
```

**Способ 2 - вручную:**
1. Скачайте установщик: https://github.com/UB-Mannheim/tesseract/wiki
2. Установите в `C:\Program Files\Tesseract-OCR`
3. **ВАЖНО:** при установке выберите компонент "Language data" (русский язык)
4. Добавьте в PATH или укажите путь в коде

#### Для Linux:
```bash
sudo apt update
sudo apt install tesseract-ocr tesseract-ocr-rus
```

#### Для Mac:
```bash
brew install tesseract tesseract-lang
```

### Шаг 4: Создание виртуального окружения и установка зависимостей

#### Windows:
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

#### Linux/Mac:
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## 🎮 Запуск

### Windows (двойной клик):
Просто запустите `run.bat`

### Вручную:
```bash
# Активируйте виртуальное окружение
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac

# Запустите программу
python main.py
```

## 📝 Первоначальная настройка

1. Запустите программу
2. Нажмите `F1` для калибровки (или запустите `python calibrate.py`)
3. Следуйте инструкциям на экране
4. Настройте координаты в `resources/config/settings.yaml` при необходимости

## 🔧 Управление

| Клавиша | Режим | Описание |
|---------|-------|----------|
| **F1** | Локация 1 | Заглушка (будет добавлено) |
| **F2** | Динозавры | Заглушка (будет добавлено) |
| **F3** | Космос | Заглушка (будет добавлено) |
| **F4** | Умный режим | Автоматические клики + окна + улучшения |
| **F6** | Свободный клик | Кликает где мышь |
| **F5** | - | Пауза/Продолжить |
| **F9** | - | Выход |

## ⚠️ Важные замечания

- **Tesseract OCR обязателен** для работы умного режима
- Запускайте программу **от имени администратора** на Windows для лучшей совместимости
- При проблемах с правами доступа на Windows, добавьте папку с игрой в исключения антивируса

## 📂 Структура проекта

```
AutoClickerCtS/
├── actions/          # Логика действий (кликер, окна, улучшения)
├── core/            # Основные классы
├── utils/           # Утилиты (OCR, изображения, конфиг)
├── resources/       # Ресурсы (конфиги, шаблоны)
├── main.py          # Точка входа
├── calibrate.py     # Калибровка координат
└── requirements.txt # Зависимости
```

## 🤝 Как внести свой вклад

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License